{% extends 'base.html.twig' %}

{% block title %}{{ "Administration" ~ parent() }}{% endblock %}

{% block body %}
    <div class="row mt-3">
        <div class="col col-lg-4">
            <div class="card text-white bg-secondary mb-3">
                <div class="card-header h3">
                    <img src="{{ asset('images/account.png') }}" alt="" width="30" height="28" class="d-inline-block">
                    Utilisateurs
                </div>
                <div class="card-body">
                    <p class="card-text">Modifier le rôle</p>
                    <a href="{{ path('admin_user') }}" class="btn btn-small btn-info">Aller voir</a>
                </div>
            </div>
        </div>

        <div class="col col-lg-4">
            <div class="card text-white bg-secondary mb-3">
                <div class="card-header h3">
                    <img src="{{ asset('images/content.png') }}" alt="" width="30" height="28" class="d-inline-block">
                    Contenu
                </div>
                <div class="card-body">
                    <p class="card-text">Modifier/Supprimer le contenu</p>
                    <a href="{{ path('admin_contenu') }}" class="btn btn-small btn-info">Aller voir</a>
                </div>
            </div>
        </div>

        <div class="col col-lg-4">
            <div class="card text-white bg-secondary mb-3">
                <div class="card-header h3">
                    <img src="{{ asset('images/admin.png') }}" alt="" width="30" height="28" class="d-inline-block">
                    Administrateur
                </div>
                <div class="card-body">
                    <p class="card-text">Ajouter/Supprimer un administrateur</p>
                    <a href="{{ path('admin_administrator') }}" class="btn btn-small btn-info">Aller voir</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
